{{- if .Values.secret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: mazebank-secrets
  namespace: {{ .Values.namespace | default .Release.Namespace }}
type: Opaque
stringData:
  MONGO_CONNECTION_URL: {{ .Values.secret.mongoConnectionUrl | quote }}
{{- end }}
