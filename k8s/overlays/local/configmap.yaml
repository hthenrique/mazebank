# App no Kind: profile local + Mongo do proprio cluster (nao Atlas).
apiVersion: v1
kind: ConfigMap
metadata:
  name: mazebank-config
  namespace: mazebank
data:
  SPRING_PROFILES_ACTIVE: local
  PORT: "8080"
  MONGO_CONNECTION_URL: mongodb://mongo:27017
